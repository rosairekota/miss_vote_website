{% extends "base.html.twig" %}

{% block body %}
<div class="container-fluid register">
                <div class="row">
                    
                    <div class="col-md-8 offset-md-2 register-right">
                        <ul class="nav nav-tabs nav-justified" id="myTab" role="tablist">
                            <li class="nav-item">
                                <a class="nav-link active" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true">Votant</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" id="profile-tab" data-toggle="tab" href="#profile" role="tab" aria-controls="profile" aria-selected="false">Jury</a>
                            </li>
                        </ul>
                        <div class="tab-content" id="myTabContent">
                            <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">
                                <h3 style="margin-left:1%;" class="register-heading">Votez pour le n°{{candidat.id}} : <span class="text-primary lead"> {{candidat.nom}}-{{candidat.prenom}}</span></h3>
                                <div class="row register-form">
                                    <div class="col-md-6">
                                       <form action="{{ path('cote_add')}}" method="post">
                                            <div class="form-group">
                                            <select class="form-control">
                                                <option class="hidden" >selectionner la voix ou cote selon vos moyens</option>
                                                {% for key,cote in cotes %}
                 
                                                <option value="{{key}}"> {{cote}} points ={{key}} $</option>
                     
                                                {% endfor %}
                                            </select>
                                        </div>
                                        <div class="form-group">
                                              <span class="text-primary lead">Nombre de fois de votes</span>
                                            <input type="number" class="form-control"  value="" />
                                        </div>
                                        <input type="submit" class="btnRegister"  value="Voter"/>
                                       </form>
                                    </div>
                                     <div class="col-md-6">
                                        <img src="{{ vich_uploader_asset(candidat,'imageFile')}}" 
									        style="width: 100%; height: 100%;">
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                     
                </div>

            </div>
    
{% endblock body %}